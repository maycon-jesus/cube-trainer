// Sentinel values used by the session/puzzle selectors to represent the
// "all" (no filter) option. Kept here so they aren't redefined per component.
export const ALL_SESSIONS = -1
export const ALL_PUZZLES = 'all'
export const ALL_TYPES = 'all'
